---
title: Creative Programming Hackathon Gallery
---

<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>

    <link rel="stylesheet" href="css/styles.css"/>
  </head>

  <body>
    <h1>{{ title }}</h1>

    <div class="projects">
      {% for project in projects %}
        <a class="project" href="{{ project.link }}" target="_blank">
          <h2>{{ project.title }}</h2>
          <div class="author">by {{ project.author }}</div>
          <div
          class="image"
          style="background-image: url({{ project.image }})"
        ></div>
        </a>
      {% endfor %}
    </div>
  </body>
</html>